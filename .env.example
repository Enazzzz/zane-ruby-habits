# Upstash Redis — required on Vercel so Zane and Ruby share one call log
# and the Last here check-in notes. Marketplace stores named upstash-kv-*
# usually inject the KV_* names.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
KV_REST_API_URL=
KV_REST_API_TOKEN=
